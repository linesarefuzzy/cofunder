<%= form_for(@cooperative) do |f| %>
  <% if @cooperative.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cooperative.errors.count, "error") %> prohibited this cooperative from being saved:</h2>

      <ul>
      <% @cooperative.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Name %><br />
    <%= f.text_field :Name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
