<!DOCTYPE html>
<html>
<head>
  <meta http-equiv="Content-Type" content="text/html; charset=utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%=h yield(:title).blank? ? '' : yield(:title) + ' | ' %>The Working World : Cofunder</title>
  <%= javascript_include_tag "bootstrap" %>
  <%= javascript_include_tag "application" %>
  <%= stylesheet_link_tag    "bootstrap", :media => "all" %>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= csrf_meta_tags %>
  <!--[if lt IE 9]>
  <script src="http://html5shim.googlecode.com/svn/trunk/html5.js"></script>
  <![endif]-->
  <%= yield :head %>
</head>

<body>
  <div id="header_group">
    <div id="header_inner_wrap">
      <div id="brand"><%= image_tag "/images/tww_logo.png", :alt => "The Working World" %></div>
      <div id="nav"><ul class="nav-ui">
        <li class="nav-item"><a class="nav_link" href="#">What We Do</a></li>
        <li class="nav-item"><a class="nav_link" href="#">Where We Work</a></li>
        <li class="nav-item"><a class="loan_list nav_link" href="<%= url_for loans_path %>">Find Loans</a></li>
        <!-- <li class="nav-item"><% link_to "Find Loans", loans_path, :class=>"loan_list" %></li> -->
        <% if user_signed_in? %>
        <li class="nav-item dropdown usermenu">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" data-target="#">
            <%= current_user.get_full_name %>
            <b class="caret"></b>
          </a>
          <ul class="dropdown-menu" role="menu">
            <li><%= link_to "Profile", edit_user_registration_path %></li>
            <li><%= link_to "Sign Out", destroy_user_session_path, :method => :delete %></li>
          </ul>
        </li>
        <% else %>
        <li class="nav-item"><a class="btn sign_in" href="<%= url_for new_user_session_path %>">Sign In</a></li>
        <% end %>
      </ul></div> 
    </div>
  </div>

  <% if notice %><p class="notice"><%= notice %></p><% end %>
  <% if alert %><p class="alert"><%= alert %></p><% end %>
  
  <%= yield %>
  
  <!-- FOOTER -->
  <div class="container">
  <footer>
    <hr/>
      <p class="pull-right"><a href="#">Back to top</a></p>
      <p>&copy;2013 The Working World, United States | <a href="#">Privacy</a> | <a href="#">Terms</a></p>
  </footer>
  </div>
</body>
</html>
